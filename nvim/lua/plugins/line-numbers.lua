-- Display both absolute and relative line numbers
return {
  "shrynx/line-numbers.nvim",
  opts = {
    -- Set custom color for the absolute line numbers
    abs_highlight = { fg = "#80A0FF" },
  },
}
